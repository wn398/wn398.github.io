<% if (site.posts.length){ %>
<div class="categorieslist">
<p class="asidetitle"><%= __('recent_updated') %></p>
  <ul class="entry">
    <% site.posts.sort('updated', -1).limit(5).each(function(post){ %>
      <li>
        <a href="<%= config.root %><%= post.path %>"><%= post.title %></a>
      </li>
    <% }); %>
  </ul>
</div>
<% } %>